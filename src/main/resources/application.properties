spring.datasource.url=jdbc:mysql://${DATABASE_HOST}:${DATABASE_PORT}/alurafood-pedidos?createDatabaseIfNotExist=true
spring.datasource.username=${DATABASE_USER}
spring.datasource.password=${DATABASE_PWD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.application.name=orders-ms
eureka.client.serviceUrl.defaultZone=${EUREKA_URI}
server.port=0

eureka.instance.instance-id=${spring.application.name}:${random.int}